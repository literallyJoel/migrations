{
  "name": "jv-migrations",
  "version": "1.1.1",
  "description": "Universal SQL migration utility for Bun & Node",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "migrate": "./dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "bun run ./src/cli.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.6.0",
    "chalk": "^5.3.0",
    "dotenv": "^17.2.3"
  },
  "dependencies": {
    "pg": "^8.12.0"
  }
}